public int  get_element(int pos)
    {
        int count =0;
        Node curr = head;

        while(curr != null)
        {
            if(count == pos)
                return curr.data;
            count++;
            curr = curr.next;
        }

        assert(false);
        return 0;

    }
