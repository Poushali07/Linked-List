public void insert_pos(Node prev, int data)
    {
        if(prev == null)
        {
            System.out.print("Previous node cannot be null");
            return;
        }
        Node new_node = new Node(data);

        new_node.next = prev.next;
        prev.next = new_node;
    }
